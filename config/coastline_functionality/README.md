# Coastline functionality

Configs the code loads to run. Edit with care.

- **config.yaml** — everyday config the CLI and API load.
- **default.yaml** — fallback when no config is given.
- **experiment.yaml** — preferred strategy config when unspecified.
- **infrastructure.yaml** — cluster GPU caps the API enforces — edit with care.
